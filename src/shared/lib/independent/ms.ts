export const Ms = {
  second: (count = 1) => count * 1000,
  minute: (count = 1) => count * 1000 * 60,
  hour: (count = 1) => count * 1000 * 60 * 60,
  day: (count = 1) => count * 1000 * 60 * 60 * 24,
}
